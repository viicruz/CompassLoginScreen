import { StyleSheet } from "react-native";
const style = StyleSheet.create({
  container: {
    marginTop: 70,
    marginBottom: 150,
    // backgroundColor: "red",
  },

  totalText: {
    color: "white",
    fontSize: 20,
    fontWeight: "bold",
  },

  acount: {
    color: "white",
    fontSize: 32,
    fontWeight: "500",
  },
});
export default style;
